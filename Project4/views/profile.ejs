<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project 4</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">

        <div class="flex justify-end">
            <a class="px-3 py-2 bg-red-500 rounded-md mb-5 inline-block" href="/logout">Logout</a>
        </div>

        <h3 class="text-3xl">Hello, <%= user.name %></h3>
        <h5 class="mb-10">You can create a new post.</h5>

        <form action="/post" method="post">
            <textarea class=" w-1/3 p-3 block outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md " placeholder="What's on your mind ?" name="content"></textarea>
            <input class="px-3 py-2 bg-blue-500 block rounded-md mt-4" type="submit" value="Create Post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-5 mb-4 border-2 border-zinc-800 rounded bg-zinc-800">
                    
                        <h4 class="text-blue-400"><%= user.username %></h4>
                    <p class="mt-4 text-sm tracking-tight"><%= post.content %></p>
                    <div class="btns flex mt-5 gap-5">
                        <a class="text-red-400" href="">Like</a>
                        <a class="text-green-400" href="">Edit</a>

                    </div> 
                    
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>