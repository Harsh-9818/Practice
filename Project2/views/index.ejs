<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project 2</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white">
        <div class="form p-10">
            <form action="/create" method="post">
                <input name="title" class="w-full rounded-lg block px-5 py-2 bg-zinc-800" type="text" placeholder="Title Goes Here !">
                <textarea name="description" class="w-full rounded-lg block px-5 py-2 bg-zinc-800 resize-none mt-2" placeholder="Enter Task Description"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-lg" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex flex-wrap gap-4">

            <% if(files.length > 0){ %>
                <% files.forEach(function(val) {%>
                    <div class="task min-w-72  px-3 py-4 rounded-lg bg-zinc-800">
                        <h1 class="text-white text-2xl"><%= val %></h1>
                        <div  class="flex w-full justify-between items-center mt-3">
                            <a class="text-blue-400 inline-block" href="/file/<%= val%>">Read More.</a>
                            <a class="text-zinc-600" href="/edit/ <%= val %>">Edit Filename</a>
                        </div>
                    </div>
                <% }) %>

            <% } else { %>
                <h3 class="text-zinc-500">No Task Yet</h3>
            <% } %>
        </div>
    </div>>
</body>
</html>